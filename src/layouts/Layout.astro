---
import i18next from "i18next";
import "../styles/index.css";

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang={i18next.language} class={"subpixel-antialiased"}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" />
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Rubik:wght@300;400;700;800&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="bg-amber-50 dark:bg-slate-800">
    <main
      class="text-black container dark:text-amber-50 p-1 md:mx-auto md:pt-2"
    >
      <slot />
    </main>
  </body>
</html>
